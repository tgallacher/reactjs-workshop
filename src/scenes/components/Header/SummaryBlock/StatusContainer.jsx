import React from 'react';

const StatusContainer = props => (
  <div className="flex flex-col flex-1 m-1/2 text-xl" {...props} />
);

StatusContainer.displayName = 'StatusContainer';

export default StatusContainer;
